# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
`package.json` pins Expo `~57.0.20` — check it before trusting this number if it drifts again.
